spring.application.name=four-year-planner
server.error.include-message=always
clerk.jwks-url=${CLERK_JWKS_URL:https://thankful-dolphin-88.clerk.accounts.dev/.well-known/jwks.json}
app.guest.cookie-name=${APP_GUEST_COOKIE_NAME:terpplanner_guest}
app.guest.cookie-domain=${APP_GUEST_COOKIE_DOMAIN:}
app.guest.cookie-secure=${APP_GUEST_COOKIE_SECURE:true}
app.guest.cookie-same-site=${APP_GUEST_COOKIE_SAME_SITE:Lax}
app.guest.session-days=${APP_GUEST_SESSION_DAYS:30}
app.guest.cleanup-inactive-days=${APP_GUEST_CLEANUP_INACTIVE_DAYS:45}
